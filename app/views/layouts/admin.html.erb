<!DOCTYPE html>
<html>
    <head>
        <title> DMSS </title>
        <%= csrf_meta_tags %>

        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>

    <body>

        <div class="container-fluid body-content">
          <div class="content-inside">

            <div class="row">
              <%= render "common/navbar" %>              
            </div>

            <%= render "common/flashes" %>

            <div class="col-md-2">
              <ul class="nav nav-pills nav-stacked" style="max-width: 300px; padding-top:20px;">
                <li class="active"><%= link_to("Records List", admin_records_path ) %> </li>
                <li class="active"><%= link_to("Statistics", admin_records_path ) %> </li>
                <li class="active"><%= link_to("User Management", admin_records_path ) %> </li>
                <li class="active"><%= link_to("Operating Room Management", admin_records_path ) %> </li>
                <li class="active"><%= link_to("Hospital Management", admin_records_path ) %> </li>
                <li class="active"><%= link_to("Backup／Recovery", admin_records_path ) %> </li>
              </ul>
            </div>

            <div class="col-md-10">
              <%= yield %>
            </div>

         </div>
        </div>

        <%= render "common/footer" %>

    </body>
</html>
